namespace Poker.Core
{
    public class Constants
    {
        /// <summary>
        /// By default the IComparer's sort by Ascending order.
        /// <para>Reverse the result to sort in Descending order.</para>
        /// </summary>
        public const int SortDirection = -1; 
    }
}